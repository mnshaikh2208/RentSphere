import { Link } from 'react-router-dom';
import { formatPrice } from '../data/listings';

export function PropertyCard({ item, saved, onSave }) { return <article className="listing-card tilt-card"><div className="card-media"><img loading="lazy" src={item.image} alt={item.title}/><span className="status">{item.availability}</span><button className={`heart ${saved ? 'saved':''}`} onClick={() => onSave(item.id)} aria-label="Save property">♥</button></div><div className="card-content"><div className="eyebrow">{item.type} · ★ {item.rating}</div><h3>{item.title}</h3><p className="location">⌖ {item.location}</p><div className="facts"><span>{item.bhk} BHK</span><span>{item.area} sq.ft</span><span>{item.furnishing}</span></div><div className="card-actions"><div><strong>₹{formatPrice(item.price)}</strong><small>/ month</small></div><Link className="text-link" to={`/properties/${item.id}`}>View details →</Link></div></div></article>; }

export function VehicleCard({ item, saved, onSave }) { return <article className="listing-card tilt-card"><div className="card-media"><img loading="lazy" src={item.image} alt={item.name}/><span className="status">{item.badge}</span><button className={`heart ${saved ? 'saved':''}`} onClick={() => onSave(item.id)} aria-label="Save vehicle">♥</button></div><div className="card-content"><div className="eyebrow">{item.kind} · ★ {item.rating}</div><h3>{item.name}</h3><p className="location">⌖ {item.location}</p><div className="facts"><span>{item.fuel}</span><span>{item.transmission}</span><span>{item.seats} seats</span></div><div className="card-actions"><div><strong>₹{formatPrice(item.price)}</strong><small>/ day</small></div><Link className="text-link" to={`/vehicles/${item.id}`}>View details →</Link></div></div></article>; }
