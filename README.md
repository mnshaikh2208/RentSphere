# RentSphere React

The redesigned React application runs from this repository root.

```bash
npm install
npm run dev
```

It includes responsive home, property and vehicle listing/detail routes, search/filter/sort controls, saved listings, gallery interactions, frontend-only local booking requests, and demo login/registration validation. The original HTML/CSS/JavaScript project is retained in `RentSphere/` as the migration source.
